# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# ASSUMPTION: one file for runtime + test deps is enough for this class project.
# FOLLOW-UP: add FastAPI, google-cloud-pubsub, etc. when those tasks land.
-e .
pydantic>=2.9,<3
python-dotenv>=1.0,<2
pytest>=8,<10
