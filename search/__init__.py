"""Search orchestration: Redis-backed iterative Exa retrieval.

This is Component B in spec.md. The package is named search because it
runs the bounded search loop. The search/ package covers cache, consolidation,
the executor, the loop, the Claude adapter, the worker, and local main.py.
"""
